apiVersion: v1
kind: Service
metadata:
  name: mongodb-external
spec:
  type: NodePort
  selector:
    app: mongodb-deployment-svc
  ports:
    - port: 27017
      targetPort: 27017
      nodePort: 30000
